<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        style="@style/TextAppearance.Material3.TitleMedium"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_weight="1"
        android:paddingStart="@dimen/history_control_padding"
        android:paddingTop="@dimen/history_control_padding"
        android:paddingEnd="@dimen/history_control_padding"
        android:paddingBottom="@dimen/history_control_padding"
        android:text="@string/time_scale"
        android:textColor="?attr/colorOnSurface" />

    <Spinner
        android:id="@+id/time_scale_spinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:paddingEnd="@dimen/history_control_padding" />

</LinearLayout>
